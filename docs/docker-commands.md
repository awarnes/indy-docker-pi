# Docker Commands

Startup system:
```
$ ./bin/boot-up.sh
```

Shutdown system:
```
$ docker-compose down
```

Start up a terminal in one of the containers:
```
$ docker exec -it indy-docker-pi_ngrok_1 /bin/sh
```